name: Android CI Clean 1.0.1

on:
  push:
    branches: ["main", "master"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "8.13"

      - name: Install Android SDK 36
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36"

      - name: Preserve debug signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: prompt-dataset-clean-debug-${{ github.repository_id }}

      - name: Verify project sources
        run: python3 scripts/verify_project.py

      - name: Build debug APK
        run: gradle --stacktrace --console=plain assembleDebug

      - name: Run local unit tests
        run: gradle --stacktrace --console=plain testDebugUnitTest

      - name: Run Android lint
        run: gradle --stacktrace --console=plain lintDebug

      - name: Prepare APK
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk dist/PromptDatasetBuilder-Clean-1.0.1-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: PromptDatasetBuilder-Clean-1.0.1-debug
          path: dist/PromptDatasetBuilder-Clean-1.0.1-debug.apk
          if-no-files-found: error

      - name: Upload diagnostic reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: PromptDatasetBuilder-Clean-1.0.1-reports
          path: |
            app/build/reports/tests/
            app/build/reports/lint-results-debug.html
            app/build/reports/lint-results-debug.txt
          if-no-files-found: ignore
